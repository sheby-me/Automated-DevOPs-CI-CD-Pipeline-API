# Automated-DevOPs-CI-CD-Pipeline-API
A RESTful API implemented using Python and Django, that automates the Continuous Integration and Continuous Deployment (CI/CD) workflow for software projects. The system accepts a Git repository URL, automatically clones the repository, performs build and testing processes, generates execution logs, and manages deployment tasks.
